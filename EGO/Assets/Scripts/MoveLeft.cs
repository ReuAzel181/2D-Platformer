using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MoveLeft : MonoBehaviour
{
    // Speed is declared as a public field, so it can be set in the Unity Inspector
    public float speed = 10f;

    // You can leave this script empty for now, as it's only needed to store the speed
}
